# Service Settings
# ~~~~~~~~~~~~~~~~
service.reading {
  startTimeout: 10.seconds
  api.admin.timeout: 10.seconds
}

# Logging Settings
# ~~~~~~~~~~~~~~~~
logging {
  gelf.facility: reading-service-admin
  level: INFO
}